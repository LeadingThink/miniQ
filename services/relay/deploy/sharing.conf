# Install as /etc/systemd/system/miniq-relay.service.d/sharing.conf.
# Persistent public snapshots, separate from encrypted remote-access payloads.
[Service]
Environment=MINIQ_SHARE_DIR=/home/ubuntu/miniq-relay/shares
